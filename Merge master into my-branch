1. # Switch to your feature branch
   git checkout my-branch

2. # Pull the latest changes from the remote master branch
   git pull origin master

3. # Merge master into your feature branch
   git merge master

4. # Resolve any conflicts if there are any
   # (After resolving conflicts)
   git add .

5. # Complete the merge with a commit
   git commit -m "Merge master into my-branch"

6. # Push the updated feature branch to the remote repository
   git push origin my-branch
